  mysql_root_password: example
  
  mysql_databases:
    - name: '{{ db_name }}'
      encoding: latin1
      collation: latin1_general_ci

  mysql_users:
    - name: '{{ db_user }}'
      host: "%"
      password: '{{ db_password }}'
      priv: "*.*:ALL"


